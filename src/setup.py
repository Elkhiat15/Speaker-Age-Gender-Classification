from setuptools import setup, find_packages

setup(
    name='packages_linker',
    version='0.1',
    packages=find_packages()
)

### This setup script is used to package the `packages_linker` module.